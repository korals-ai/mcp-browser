{
  "name": "mcp-browser-bridge",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Browser extension that lets the browser tool's MCP server drive tabs you choose in your own browser. Load `dist/` unpacked.",
  "license": "Apache-2.0",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "node build.mjs",
    "typecheck": "tsc --noEmit",
    "test": "node build.mjs --test && node --test tests/*.test.mjs"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.287",
    "esbuild": "^0.24.0",
    "typescript": "^5.6.0"
  }
}
